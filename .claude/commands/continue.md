---
description: Alias of /build — pick the next TODO and implement it.
allowed-tools: Read, Bash, Edit, Write, Glob, Grep, Task
---

Same behavior as `/build`. Provided so the user can type "continue" naturally.
Execute the Build loop documented in `CLAUDE.md`.
